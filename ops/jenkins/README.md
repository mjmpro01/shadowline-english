# Jenkins on the Shadowline VPS

```bash
docker compose -f docker-compose.jenkins.yml up -d --build
```

Full setup (webhook, credentials, RAM, backup, Portainer profile):
[`docs/ops-jenkins.md`](../../docs/ops-jenkins.md).
